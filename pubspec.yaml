name: dukaapp
description: "DukaApp - POS"
publish_to: 'none'
version: 1.0.0+107

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Routing
  go_router: ^14.8.1

  # Design
  google_fonts: ^6.2.1
  flutter_screenutil: ^5.9.3
  flutter_svg: ^2.0.17
  responsive_framework: ^1.5.1

  # Networking
  dio: ^5.7.0
  internet_connection_checker_plus: ^2.7.3

  # Secure Storage
  flutter_secure_storage: ^9.2.4

  # Local Storage
  shared_preferences: ^2.3.4

  # Offline-first local database (unique DB per role+shop)
  sqflite: ^2.3.3+1

  # App version info
  package_info_plus: ^8.0.0

  # In-app update (Google Play Core)
  in_app_update: ^5.0.0

  # Biometric authentication
  local_auth: ^2.3.0

  # Utilities
  intl: ^0.19.0
  flutter_animate: ^4.5.2
  path_provider: ^2.1.5
  open_file: ^3.5.4
  share_plus: ^10.1.4
  url_launcher: ^6.3.1

  # File Parsing
  excel: ^4.0.6
  csv: ^6.0.0

  # PDF
  pdf: ^3.11.1

  # Camera & Media
  mobile_scanner: ^7.4.0
  image_picker: ^1.2.3
  file_picker: ^8.0.0

  # PDF Preview & Printing
  printing: ^5.13.4


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.4.14
  riverpod_generator: ^2.6.3
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/submark_logo.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/images/submark_logo.png"
  remove_alpha_ios: true
  padding: 0.25

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/icons/
    - assets/images/
    - assets/logos/
    - assets/animations/
